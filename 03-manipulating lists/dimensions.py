# 定义元组
dimensions = (200, 50)
print(dimensions[0])
print(dimensions[1])

# dimensions[0] = 250  元组元素不可以修改


my_t = (2,)  # 定义只包含一个元素的元组，“，”元组是由逗号标识的

print('\n')
dimensions_1 = (270, 50)
for dimension in dimensions_1:
    print(dimension)