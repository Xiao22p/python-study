motorcycles = ['honda','yamaha','suzuki']  # 首先定义一个列表motorcycles，并将其打印
print(motorcycles)

popped_motorcycles = motorcycles.pop()# 接着使用pop()方法删除（弹出）末尾元素，然后将其赋给变量popped_motorcycles
print(motorcycles) # 核实确实删除了末尾元素
print(popped_motorcycles)  # 证明可以接着使用被删除（弹出）的元素
